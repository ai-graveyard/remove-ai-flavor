---
globs: "*.md,*.mdc"
alwaysApply: false
description: Markdown 文档格式和维护规范
---

# Markdown 规范

- 每个文档只有一个一级标题，标题层级连续。
- 使用 `-` 作为无序列表符号，嵌套列表保持一致缩进。
- 代码块必须标注语言；目录树使用 `text`，环境变量使用 `dotenv`。
- 文件链接优先使用相对路径，图片必须有有意义的 alt 文本。
- 命令必须与当前 Makefile、package scripts 和项目目录一致。
- Markdownlint 与 Prettier 通过 Makefile 临时运行；使用 `make lint-md` 检查，使用 `make lint-md-fix` 或 `make format-md` 修复，无需安装根目录 Node.js 依赖。
- 文档工具忽略 vendored 的 `api/app/skills/stop-slop/`，避免改写上游固定副本。
- 中英文 README 的功能、端口、命令和安全说明保持同步。
- 配置示例只使用占位值，不放入真实密钥、域名或用户数据。
- 历史计划和上游 Skill 资料保留原始语义，不因产品文档更新而重写。

提交前检查：

- 标题、列表、代码围栏和相对链接可正确渲染。
- 文件路径、端口、环境变量和 API 路径能在仓库中找到依据。
- 删除已经不存在的模块、命令和部署目录引用。
- 行为变化已同步更新根 README、子项目 README、`AGENTS.md` 或相关规则。
