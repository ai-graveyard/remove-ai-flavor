---
description: Docker Compose、数据库迁移和生产部署规范
---

# 部署与运维

## 真实入口

- 完整部署位于 `deploy/`，没有独立 `deploy-test/` 目录。
- 测试环境通过 `deploy/.env.test.example` 和后缀变量隔离。
- 所有部署目标统一定义在根 Makefile 中，`deploy/` 只保存 Compose、Nginx 和环境配置。
- 对外入口默认是 Nginx `8081`；Web 容器为 `3000`，API 容器为 `8000`。

## 命令

```bash
cp deploy/.env.example deploy/.env
make build-all
make start
make logs
make stop
make deploy-ci
```

使用 `docker compose`，不写旧式 `docker-compose`。实际目标名是 `start` 和 `stop`，不是 `up` 和 `down`。

## 配置

- 使用 `AUTH_SECRET_KEY`，不使用 `SECRET_KEY`。
- 数据库使用拆分的 `POSTGRES_*`，不使用 `DATABASE_URL`。
- 邮件使用 `MAIL_*` 或 `RESEND_*`。
- 模型使用 `AGENT_API_KEY`、`AGENT_BASE_URL` 和 `AGENT_MODEL_NAME`。
- 不提交 `.env`，生产配置不得保留示例密码或开启调试。

## 迁移与健康检查

```bash
docker compose --project-directory deploy -f deploy/docker-compose.yaml exec api alembic upgrade head
docker compose --project-directory deploy -f deploy/docker-compose.yaml exec api wget -qO- http://127.0.0.1:8000/health
docker compose --project-directory deploy -f deploy/docker-compose.yaml ps
```

应用启动时的 `create_all` 不能替代 Alembic。升级前备份 PostgreSQL，并在测试环境验证迁移和回滚。

GitHub Actions 通过 `.github/workflows/deploy.yml` 使用 SSH 运行 `make deploy-ci`。必须校验 `known_hosts`，私钥只放在 Actions Secrets 中。

## 安全

- 生产环境设置 `AUTH_IS_DEBUG=False`、`API_RELOAD=False`。
- 使用 HTTPS，限制 PostgreSQL 与 Redis 的公网端口。
- 对 `.env`、备份和 `volumes/` 设置最小文件权限。
- 日志不得包含密钥、用户原文或上游响应正文。
- 轮换 JWT、模型、邮件和 Stripe 凭据，并建立数据库恢复演练。
