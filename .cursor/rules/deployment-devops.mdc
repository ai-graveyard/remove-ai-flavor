---
description: 部署和运维规范，包含 Docker、CI/CD 和监控最佳实践
---

# 部署和运维规范

## Docker 部署

### 核心文件

- **生产环境**: [deploy/docker-compose.yaml](mdc:deploy/docker-compose.yaml)
- **测试环境**: [deploy-test/docker-compose.yaml](mdc:deploy-test/docker-compose.yaml)
- **后端 Dockerfile**: [api/Dockerfile](mdc:api/Dockerfile)
- **前端 Dockerfile**: [web/Dockerfile](mdc:web/Dockerfile)

### 1. Docker Compose 使用

```bash
# 进入部署目录
cd deploy  # 生产环境
cd deploy-test  # 测试环境

# 启动所有服务
make up

# 停止所有服务
make down

# 查看日志
make logs

# 重新构建并启动
make rebuild

# 查看服务状态
docker-compose ps
```

### 2. 环境配置

**生产环境 `.env` 文件**:

```bash
# 数据库
POSTGRES_USER=postgres
POSTGRES_PASSWORD=strong_password
POSTGRES_DB=app_db

# Redis
REDIS_PASSWORD=redis_password

# 后端
SECRET_KEY=your-secret-key
DATABASE_URL=postgresql://user:pass@postgres:5432/db
REDIS_URL=redis://:password@redis:6379/0

# AI
AGENT_API_KEY=sk-xxxxx

# 邮件
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# 前端
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
```

### 3. 健康检查

```yaml
# docker-compose.yaml
services:
  api:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
```

## 数据库管理

### 1. 迁移

```bash
# 在容器中执行迁移
docker-compose exec api alembic upgrade head

# 创建新迁移
docker-compose exec api alembic revision --autogenerate -m "description"
```

### 2. 备份和恢复

```bash
# 备份数据库
docker-compose exec postgres pg_dump -U postgres app_db > backup.sql

# 恢复数据库
docker-compose exec -T postgres psql -U postgres app_db < backup.sql
```

## 监控和日志

### 1. 日志查看

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f api
docker-compose logs -f web

# 查看最近 100 行
docker-compose logs --tail=100 api
```

### 2. 应用监控

- 使用结构化日志
- 记录关键操作和错误
- 监控 API 响应时间
- 监控数据库连接池

## 性能优化

### 1. 后端优化

- 使用数据库连接池
- 实现 Redis 缓存
- 优化数据库查询 (添加索引)
- 使用异步处理长时间任务

### 2. 前端优化

- 使用 Next.js 静态生成 (SSG)
- 实现代码分割
- 图片优化 (Next.js Image)
- 使用 CDN

## 安全规范

### 1. 生产环境检查清单

- [ ] 使用强密码
- [ ] 配置 HTTPS
- [ ] 限制数据库访问
- [ ] 启用防火墙
- [ ] 定期更新依赖
- [ ] 配置日志轮转
- [ ] 实现速率限制
- [ ] 定期备份数据

### 2. 密钥管理

- 不要在代码中硬编码密钥
- 使用环境变量
- 定期轮换密钥
- 使用密钥管理服务 (如 AWS Secrets Manager)

## CI/CD

### GitHub Actions 示例

```yaml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Build and push Docker images
        run: |
          docker-compose build
          docker-compose push
      
      - name: Deploy to server
        run: |
          ssh user@server 'cd /app && docker-compose pull && docker-compose up -d'
```

## 常见问题

### Q: 如何更新生产环境?

```bash
cd deploy
git pull
docker-compose pull
docker-compose up -d
```

### Q: 如何查看服务资源使用?

```bash
docker stats
```

### Q: 如何清理未使用的 Docker 资源?

```bash
docker system prune -a
```
