---
description: 认证、密钥、隐私和权限边界规范
---

# 安全规范

## 认证与权限

- JWT 使用 `AUTH_SECRET_KEY`、`AUTH_ALGORITHM` 和配置的过期时间。
- 生产环境必须关闭 `AUTH_IS_DEBUG`，不得使用示例验证码或默认密钥。
- 所有用户资源查询必须按当前用户过滤；管理接口必须验证管理员身份。
- Agent 访问遵循 `free < monthly < yearly`，创建聊天、发送消息、测试连接和文本优化都必须检查权限。
- 访客接口只允许免费 Agent，并校验 `X-Guest-ID` 为 UUID。

## 敏感数据

- API Key、JWT、数据库密码、邮件凭据和 Stripe 密钥只从环境变量或服务端数据库读取。
- 普通用户 Agent Schema 不得包含 `api_key`；新增响应模型时显式列出公开字段。
- 日志不得记录用户原文、验证码、Token、API Key、完整上游响应或带凭据的 URL。
- 不提交 `.env`、数据库转储、生产日志或真实用户数据。

## 输入与外部服务

- 使用 Pydantic/SQLModel 验证长度、枚举、UUID 和必填字段。
- SQL 查询使用 SQLModel/SQLAlchemy 参数化表达式，不拼接用户输入。
- 外部模型、邮件和支付错误转换为本地化通用消息，内部异常只记录脱敏类型。
- 访客额度使用 Redis 原子操作；失败时归还预占，避免并发绕过限制。
- 任何重定向和回调地址都必须限制为预期来源。

## 部署

- 使用 HTTPS，限制 PostgreSQL 和 Redis 的公网访问。
- `.env`、备份和数据卷采用最小权限并定期轮换凭据。
- 依赖升级后运行测试，安全修复不得通过跳过验证或关闭保护实现。
