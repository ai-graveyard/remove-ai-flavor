---
description: Agno、OpenAI-compatible 模型与 stop-slop Skill 开发规范
---

# AI 与 Agent 开发

## 唯一运行时

- 所有模型调用统一经过 `api/app/agents/agno.py`。
- 不重新引入 `llm.py`、`dify.py` 或路由内直接调用模型 SDK。
- `AgentSource` 只控制后台标签和 URL 预设；所有来源都使用 Agno `OpenAILike`。
- 支持 OpenAI-compatible 基础地址和以 `/chat/completions` 结尾的完整地址。

## stop-slop Skill

- Skill 位于 `api/app/skills/stop-slop/`，启动时严格校验必需文件。
- 规则与参考资料直接注入系统指令，以兼容不支持工具调用的提供商。
- 保留原文事实、观点、语言、Markdown 和必要术语，不得杜撰。
- 只返回改写正文，不添加解释、评分、标题或结束语。
- 除同步上游任务外，不修改 Skill 的许可证、来源和参考资料。

## 配置与安全

- 默认配置来自 `AGENT_API_KEY`、`AGENT_BASE_URL`、`AGENT_MODEL_NAME` 和 `AGENT_MODEL_TEMPERATURE`。
- 数据库 Agent 可通过 `model_conf` 覆盖受支持的模型参数。
- API Key 只在服务端使用；普通用户 Schema 不得包含密钥。
- 日志只记录 Agent ID、模型名、Token 数和异常类型，不记录原文、密钥或上游响应正文。
- 外部错误转换为本地化、可公开的消息，不向客户端泄露内部细节。

## 生成与额度

- 文本优化是无状态改写，只处理最后一条用户正文。
- 登录对话支持流式输出，并在结束时记录真实 Token 用量。
- 流式请求开始前冻结 Agent 配置，避免持有已关闭 Session 的 ORM 对象。
- 访客仅可使用免费 Agent；预占、提交和失败归还必须保持原子性。
- 登录用户调用前必须执行会员 Agent 权限和用量限制检查。

## 测试

- Mock Agno Agent 或 OpenAI-compatible 边界，单元测试不得调用真实模型。
- 覆盖缺失配置、URL 归一化、空响应、流式事件、Token 映射和连接测试。
- 覆盖日志与响应不泄露 API Key、用户正文和上游错误正文。
- 修改 Skill 加载时覆盖文件缺失、校验失败和唯一 Skill 名称。
