---
alwaysApply: true
description: Remove AI Flavor 项目结构和技术栈
---

# 项目结构

Remove AI Flavor（RAIF）是文本去 AI 味应用，不是通用 Agent 模板。

## 技术栈

- 前端：Next.js 15.3、React 19、TypeScript、Tailwind CSS 4、Shadcn UI、next-intl。
- 后端：Python 3.12、FastAPI、SQLModel、PostgreSQL、Redis、Alembic。
- AI：Agno `OpenAILike` + OpenAI-compatible 模型 + 内置 stop-slop Skill。
- 工具：前端 pnpm，后端 uv，部署使用 Docker Compose 与 Nginx。

## 目录

- `api/app/agents/agno.py`：统一模型适配层。
- `api/app/skills/stop-slop/`：随服务发布的写作 Skill。
- `api/app/routers/v1/`：认证、聊天、优化、会员、订单和管理 API。
- `api/app/services/`：业务规则；`api/app/crud/`：数据库访问。
- `api/app/models/` 与 `api/app/schemas/`：SQLModel 和 Pydantic 模型。
- `api/alembic/versions/`：迁移；`api/tests/`：pytest 测试。
- `web/app/[locale]/`：页面；`web/app/messages/`：中英文翻译。
- `web/components/web/`：产品 UI；`web/components/admin/`：管理后台。
- `web/util/`：认证、访客和任务工具。
- `deploy/`：Compose、Nginx 和环境模板。

## 跨层要求

- 数据库模型变化必须附带 Alembic 迁移。
- 用户文案必须同步更新中英文翻译。
- 前后端请求与响应类型必须保持一致。
- 不得绕过 Agno、stop-slop、会员权限或访客额度逻辑。
- 公共代码使用中文 docstring/JSDoc，复杂业务逻辑添加中文注释。
