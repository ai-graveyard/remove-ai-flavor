---
description: pytest 与 Vitest 测试规范
---

# 测试规范

## 原则

- 测试行为和业务边界，不绑定无关实现细节。
- 每个测试独立、可重复，不依赖执行顺序或真实外部服务。
- Bug 修复优先添加失败用例，再完成修复。
- 同时覆盖成功、拒绝、异常和边界输入。

## 后端

后端使用 pytest，测试位于 `api/tests/`：

```bash
cd api
uv run pytest
uv run pytest tests/test_guest_usage_service.py
```

- Mock 模型、邮件、Stripe 和网络请求。
- Redis 业务可使用明确的 fake/stub；涉及 Lua 原子语义时补充集成验证。
- 数据库测试使用隔离 Session，不复用开发或生产数据。
- Agent 测试覆盖配置、Skill 加载、流式输出、Token 和脱敏。
- 迁移测试检查 revision 链、`upgrade` 和关键 `downgrade` 行为。

## 前端

前端使用 Vitest，测试与模块同目录，命名为 `*.test.ts` 或 `*.test.tsx`：

```bash
cd web
pnpm test
pnpm build
```

- 优先测试无 DOM 依赖的纯工具函数。
- 浏览器存储测试必须清理全局状态。
- 覆盖访客额度、登录跳转、新任务确认和草稿恢复。
- UI 改动至少通过生产构建和 TypeScript 检查。

## 提交前

- 先运行最小相关测试，再根据影响范围运行完整套件。
- 不删除或放宽断言来掩盖失败。
- 新增依赖前确认现有 pytest/Vitest 工具无法满足需求。
- 测试失败时报告具体命令和失败原因，不声称未运行的检查已通过。
