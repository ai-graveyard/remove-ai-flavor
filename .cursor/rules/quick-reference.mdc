---
alwaysApply: true
description: Remove AI Flavor 关键文件和常用命令
---

# 快速参考

## 关键文件

- API 入口：`api/app/main.py`
- Agno 适配：`api/app/agents/agno.py`
- 文本优化：`api/app/routers/v1/text_optimizer.py`
- 对话流：`api/app/routers/v1/chat.py`
- 访客额度：`api/app/services/guest_usage_service.py`
- 默认数据：`api/app/db/base.py`
- Web 入口：`web/app/[locale]/page.tsx`
- 优化编辑器：`web/components/web/editor/remove-flavor-editor.tsx`
- 侧边栏：`web/components/web/sidebar/app-sidebar.tsx`
- 翻译：`web/app/messages/{zh,en}.json`

## 命令

```bash
# 根目录开发
make dev-api
make dev-web
make deploy-ci
pnpm lint:md

# 后端
cd api
uv sync
uv run alembic upgrade head
make test
make lint

# 前端
cd ../web
pnpm install
pnpm test
pnpm build
make i18n-check

# 部署
cd ..
make build-all
make start
make logs
make stop
```

Web 开发端口为 `3009`，API 为 `8000`，OpenAPI 为 `/api/v1/docs`，健康检查为 `/health`。

排错时检查 `POSTGRES_*`、`REDIS_*`、`AUTH_SECRET_KEY` 和 `AGENT_*`；项目不使用 `DATABASE_URL`、`SECRET_KEY`、`llm.py` 或 `dify.py`。
